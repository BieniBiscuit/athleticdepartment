# Website Project

A simple website project with a "Hello World" page.

## Getting Started

Open `index.html` in a web browser or use a local development server.

### Quick Start
- Open the `index.html` file directly in your browser
- Or use a local server like Python's built-in server:
  ```bash
  python -m http.server 8000
  ```

Then visit `http://localhost:8000` in your browser.

## Project Structure
```
website/
├── index.html      # Main page with "Hello World"
└── README.md       # This file
```
